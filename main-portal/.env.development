# API基础地址（留空表示使用当前访问主机，并通过 Vite 代理转发）
VITE_API_BASE_URL=

# WebSocket基础地址（留空表示根据当前访问主机自动推导）
VITE_WS_BASE=

# 开发环境也使用登录模式（public API 需携带Token）
VITE_PUBLIC_API_ANONYMOUS=false

# PM2高危操作确认头（开发默认）
VITE_PM2_CONFIRM_TOKEN=CONFIRM

